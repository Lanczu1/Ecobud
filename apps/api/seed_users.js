const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { for (let i = 1; i <= 6; i++) { const email = 'testuser' + i + '@ecobud.app'; const existing = await prisma.user.findUnique({where: {email}}); if (existing) continue; const user = await prisma.user.create({ data: { name: 'Eco Explorer ' + i, email, passwordHash: 'dummyhash', role: 'user', status: 'active', points: 300 - (i * 20), currentStreak: 1, profile: { create: { displayName: 'Eco Explorer ' + i } }, stats: { create: { currentStreak: 1, ecoPoints: 300 - (i * 20), ecoCoins: 50, knowledgePoints: 10 } } } }); console.log('Created:', user.name); } } main().catch(console.error).finally(() => prisma.$disconnect());
